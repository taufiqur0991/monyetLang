$a = 20;
$b = $a * 2 + 5;

echo $b;

$name = "monyet";
echo "hello " + $name;

$a = 10;
if ($a > 5) {
	echo "besar";
}

function add(a, b) {
    return a + b;
}

echo add(14, 5);


function cekAngka(n) {
    if ($n > 10) {
        echo "Angka besar";
    } else {
        echo "Angka kecil";
    }
}

cekAngka(15);
cekAngka(8);
include "include.nyet";
$total = hitungPajak(10000);
echo "Total harga setelah pajak: " + $total;

    $user = [
        "nama" => "Budi",
        "umur" => 25,
        "is_admin" => 1 == 1
    ];
$result = json_encode($user);
echo $result;
echo json_decode($result);
$user = ["nama" => "Budi", "skor" => 100];
// Simpan map/array sebagai string JSON
set_data("profil_1", json_encode($user)); 

// Saat ambil, jangan lupa di-decode
$dataString = get_data("profil_1");
$profil = json_decode($dataString);

echo $profil["nama"];